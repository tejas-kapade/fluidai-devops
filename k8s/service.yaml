apiVersion: v1
kind: Service
metadata:
  name: fluidai-service
  namespace: fluidai
  labels:
    app: fluidai-app
spec:
  selector:
    app: fluidai-app
  ports:
  - name: http
    port: 80
    targetPort: 5000
  type: LoadBalancer
